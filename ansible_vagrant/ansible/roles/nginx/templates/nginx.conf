worker_processes  1;

events {
    worker_connections  1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    charset UTF-8;

    upstream unicorn {
    server unix:/var/www/grizzly/current/tmp/sockets/unicorn.grizzly.sock;
    }

    sendfile        on;

    keepalive_timeout  65;

    server {
        listen       80;
        server_name  192.168.33.40;
        location / {
            proxy_pass http://unicorn;
        }
    }
    include servers/*;
}
