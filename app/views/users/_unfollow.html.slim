= form_for(current_user.active_relationships.find_by(follow_id: @user.id), html: { method: :delete }, remote: true, authenticity_token: true) do |f|
  = f.submit 'アンフォロー', class: 'btn btn-large btn-primary'
